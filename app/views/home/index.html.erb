<h3>Home</h3>
<% @users.each do |user| %>
  <p>User: <%= user.name %> </p>
<% end %>

<% if user_signed_in? %>
  <li>
  <%= link_to("Logout", destroy_user_session_path, :method => :delete) %>        
  </li>
 <% end %>