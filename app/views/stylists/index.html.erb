<h1 class="header-title">Suggested Stylists</h1>

<div class="customer-help">
  Upinhair uses a complex algorithm to determine the best stylists near you using a combination of your personal information.
</div>

<table>
  <tr>
    <th></th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @stylists.each do |stylist| %>
  <% stylist_user = stylist.user %>
  <tr>
    <!-- <td><%= image_tag stylist_user.pic.url, :id=>"profile-avatar" %></td> -->
    <td><%= stylist.salon %></td>
    <td><%= link_to 'Show', stylist, :class=>'actions' %></td>
    <td><%= link_to 'Book It', {:controller=>"appointments", :action=>"new", :stylist => stylist.id}, :class=>'actions' %>
  </tr>
<% end %>
</table>
